# Windows-first runtime extras.
# Install after requirements.txt:
#   .venv\Scripts\python.exe -m pip install -r requirements.txt -r requirements-windows.txt

# Lightweight local OCR for Windows desktop packaging.
rapidocr-onnxruntime>=1.3.24

# Image preprocessing used by the OCR cascade.
pillow>=10.4.0

# Package the FastAPI backend as a Windows sidecar executable in a later build step.
pyinstaller>=6.11.1
